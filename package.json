{
  "name": "ss2025_ccl_cc241045",
  "private": true,
  "version": "1.0.0",
  "description": "PatPat - root manifest so Vercel can bundle the Express API in /api as a serverless function. The app itself still lives in backend/, and backend/package.json stays the manifest for running it locally.",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend start"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "body-parser": "^2.2.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1",
    "mysql2": "^3.14.1",
    "socket.io": "^4.8.1"
  }
}
