# The default matches Express's /api routes, served through Vite's local proxy.
VITE_API_BASE_URL=/api
# Never enable example data for a live deployment.
VITE_DEMO_MODE=false
# Optional if the Socket.IO backend is on a different origin.
# VITE_SOCKET_URL=http://localhost:3000
